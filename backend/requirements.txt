# ForgeMind Backend — Python dependencies
#
# This file intentionally contains ONLY what is needed for the
# scaffolding stage: the web framework, ASGI server, settings
# management, and testing tools. Database drivers, auth libraries,
# and AI/ML packages will be added in later stages as those features
# are implemented.

# --- Web framework & server -------------------------------------------
fastapi==0.115.0
uvicorn[standard]==0.30.6

# --- Configuration -------------------------------------------------------
pydantic==2.9.2
pydantic-settings==2.5.2

# --- Testing ---------------------------------------------------------------
pytest==8.3.3
httpx==0.27.2
