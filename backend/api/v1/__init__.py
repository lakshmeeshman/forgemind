"""
api/v1/
--------
Version 1 of the ForgeMind API.

Each file in this package defines a FastAPI APIRouter for one resource
or concern (e.g. health.py for health checks). All routers are combined
in router.py, which is what main.py actually imports.
"""
